// Sidenav
const sideNav = document.querySelector('.sidenav');
M.Sidenav.init(sideNav, {});

// Slider - Indicator false takes away the dots at the bottom. height sets it to 500px. transition has it switch at a 500ms speed. interval changes the picture every 6000ms
const slider = document.querySelector('.slider');
M.Slider.init(slider, {
    indicators: false,
    height: 500,
    transition: 500,
    interval: 6000

});

// Gallery - Lets you click on a picture and enlarges it
const mb = document.querySelectorAll('.materialboxed');
M.Materialbox.init(mb, {});

// ScrollSpy - Suppose to let me click in the navigation bar to click to a certain section, however, can't get it to work currently
const ss = document.querySelectorAll('.scrollspy');
M.ScrollSpy.init(ss, {});